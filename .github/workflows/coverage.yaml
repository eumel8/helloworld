name: Coverage (template)

on:
  pull_request:
  push:
    branches:
      # It's important that the action also runs on merge to main
      - main

jobs:
  call-workflow:
    uses: eumel8/workflow-templates/.github/workflows/coverage.yaml@0.0.7
